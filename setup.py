from setuptools import setup

setup(
    name="ProtoCol",
    version="0.1.0",
    description="Application for controlling a Protogen head",
    author="MarchingTrombonist",
    packages=["ProtoCol"],  # same as name
    install_requires=["wheel"],
)
